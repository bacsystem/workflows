{
  "name": "parallel-plan-executor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node --test tests/",
    "build": "node scripts/build-workflow.js"
  }
}
