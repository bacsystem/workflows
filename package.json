{
  "name": "parallel-plan-executor",
  "version": "0.6.8",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-workflow.js"
  }
}
